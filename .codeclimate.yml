version: '2'

plugins:
  python:
    enabled: true
  pylint:
    enabled: true
  bandit:
    enabled: true
  codeclimate-pytest:
    enabled: true
  codeclimate-python:
    enabled: true

checks:
  duplication:
    enabled: true
    config:
      languages:
        - python
    exclude_paths:
      - 'tests/**'
      - '**/.venv/**'
      - '**/myenv/**'
